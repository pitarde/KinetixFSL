package com.example.kinetixfsl.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * KinetixFSL brand palette.
 * Every value here is sampled directly from the logo — do not invent new ones.
 */

// Core brand
val KinetixInk = Color(0xFF1E224F)      // hand outline — darkest, body text
val KinetixNavy = Color(0xFF303465)     // main ring + wordmark — primary
val KinetixIndigo = Color(0xFF3C3489)   // right arc — secondary
val KinetixGreen = Color(0xFF1D9E75)    // top arc — success / correct sign
val KinetixMint = Color(0xFF95D4CC)     // bottom arc — tertiary / progress fills

// Tints for containers and surfaces
val KinetixNavy10 = Color(0xFFE7E8F0)
val KinetixIndigo10 = Color(0xFFE9E7F3)
val KinetixGreen10 = Color(0xFFE4F4EE)
val KinetixMint20 = Color(0xFFE9F6F4)

// Neutrals
val KinetixWhite = Color(0xFFFFFFFF)
val KinetixSurface = Color(0xFFFAFAFC)
val KinetixOutline = Color(0xFFD6D6E0)
val KinetixMuted = Color(0xFF7D809E)

// Feedback
val KinetixError = Color(0xFFC0392B)
val KinetixError10 = Color(0xFFF9E6E4)

// Dark scheme surfaces (derived from Ink, not guessed)
val KinetixDarkBackground = Color(0xFF14162F)
val KinetixDarkSurface = Color(0xFF1E224F)